## `web/`

Browser wrapper around the shared app.

- Vite app root (Vercel-friendly)
- Supabase integration (auth/projects/storage)
- CDN / deployment config

UI must remain identical; this layer only provides runtime services/adapters.

