/**
 * Render engine facade — re-exports stable contracts (no duplicate implementations).
 */

export { resolvePlaybackTimeline, sceneIndexAtTime, seekSecondsForScene } from '../engines/timeline/resolvePlaybackTimeline.js'
