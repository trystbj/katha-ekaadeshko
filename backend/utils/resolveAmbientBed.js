/**
 * @deprecated Import `storyAudioCatalog.js` for extensions — kept for stable import paths.
 */
export { resolveAmbientBedUrl, MUSIC, mixkitSfxPreviewUrl, SFX_BY_TAG } from './storyAudioCatalog.js'
