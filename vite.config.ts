/**
 * Default Vite config at repo root — required so Vercel’s “Vite” preset and plain `vite build`
 * use the same app root (`web/`), aliases, and **web-dist** output as `vite.web.config.ts`.
 */
export { default } from './vite.web.config'
